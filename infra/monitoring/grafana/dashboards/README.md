# Grafana Dashboards

This directory will contain Grafana dashboard JSON files for the monitoring stack.

Current status:

- Prometheus datasource provisioning is added.
- Dashboard provisioning is added.
- Real dashboards will be added after application and infrastructure metrics are available.

Planned dashboards:

- Backend API health
- User operations
- Realtime Socket.IO health
- PostgreSQL health
- Docker infrastructure