{
  "extends": ["//"],
  "tasks": {
    "typecheck": {
      "inputs": ["prisma/schema.prisma", "src/**", "tsconfig.json"]
    },
    "lint": {
      "inputs": ["src/**", "eslint.config.ts"]
    },
    "lint:check": {
      "inputs": ["src/**", "eslint.config.ts"]
    },
    "format": {
      "inputs": ["src/**"]
    },
    "format:check": {
      "inputs": ["src/**"]
    }
  }
}
